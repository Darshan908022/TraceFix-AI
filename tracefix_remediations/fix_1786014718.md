## 🚨 Automated Observability Alert & Fix

**Root Cause:**
The `decode_jwt` function returned `None` (likely due to an invalid, malformed, or expired JWT), causing an `AttributeError` when `app/utils/jwt.py` attempted to call `.get()` on `token_claims` without validating that it was not `None`.

**Suggested Fix:**
In `app/utils/jwt.py`, check if `token_claims` is `None` before attempting to extract claims, or raise an authentication exception. Example fix:

if not token_claims:
    raise UnauthorizedException('Invalid or expired token')
role = token_claims.get('role', '').upper()

**Severity Score:** 85/100
**Estimated Downtime Cost:** $500/hour

---
*Generated automatically by TraceFix AI Engine.*
